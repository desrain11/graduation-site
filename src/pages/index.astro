---
import Layout from '../layouts/Layout.astro';
import Sidebar from '../components/Sidebar.astro';
import { formatProjectDate, getProjectsByCategory, getSectionText } from '../lib/content';

const { main, course, extracurricular } = getProjectsByCategory();

const textPages = [
	{ id: 'identity', group: 'piv', heading: 'Identity', paragraphs: getSectionText('identity') },
	{ id: 'vision', group: 'piv', heading: 'Vision.', paragraphs: getSectionText('vision') },
];
const tailTextPages = [
	{ id: 'expertise', group: 'expertise', heading: 'Expertise Areas.', paragraphs: getSectionText('expertise-areas') },
	{ id: 'development', group: 'development', heading: 'Development.', paragraphs: getSectionText('development') },
];

const pageNames: Record<string, string> = {
	cover: 'Cover',
	identity: 'Identity',
	vision: 'Vision',
	'project-main': 'Project / Main',
	'project-course': 'Project / Course',
	'project-extracurricular': 'Project / Extracurricular',
	expertise: 'Expertise Areas',
	development: 'Development',
	contract: 'Contract',
};
const pageFlow = Object.keys(pageNames);

const coverMenu = [
	{ label: 'PI&V', target: 'identity' },
	{ label: 'Project', target: 'project-main' },
	{ label: 'Expertise Areas', target: 'expertise' },
	{ label: 'Development', target: 'development' },
	{ label: 'Contract', target: 'contract' },
];

const contactCards = [
	{ id: 'wechat', title: 'Wechat', detail: 'blueowl_fifi', qr: '/contact/QR-Wechat.svg' },
	{ id: 'email', title: 'Email', detail: 'fiahu301@gmail.com', qr: '/contact/QR-Email.svg', href: 'mailto:fiahu301@gmail.com' },
	{ id: 'linkedin', title: 'Linkedin', detail: 'yunfei-fia-hu', qr: '/contact/QR-Linkedin.svg', href: 'https://www.linkedin.com/in/yunfei-fia-hu-813b8b2b0/' },
	{ id: 'dribbble', title: 'Dribbble', detail: 'Blueowlfifi', qr: '/contact/QR-Dribble.svg', href: 'https://dribbble.com/Blueowlfifi' },
];
---

<Layout>
	<div class="site is-cover" data-site>
		<Sidebar main={main} course={course} extracurricular={extracurricular} />

		<header class="topbar" data-topbar>
			<div class="lang-switch" data-lang-switch>
				<span class="lang-en">EN</span>
				<span class="lang-divider">|</span>
				<button class="lang-cn" type="button" aria-label="中文版">中</button>
				<span class="lang-tip" role="status" aria-live="polite">敬请期待</span>
			</div>
		</header>

		<div class="track" data-track>
			<!-- ====== 封面 ====== -->
			<section class="page page-cover" data-page="cover">
				<img class="cheese cheese-lt" src="/image/cover/Cheese%20Hole%20LT.svg" alt="" aria-hidden="true" />
				<img class="cheese cheese-rt" src="/image/cover/Cheese%20Hole%20RT.svg" alt="" aria-hidden="true" />
				<img class="cheese cheese-lb" src="/image/cover/Cheese%20Hole%20LB.svg" alt="" aria-hidden="true" />
				<img class="cheese cheese-rb" src="/image/cover/Cheese%20Hole%20RB.svg" alt="" aria-hidden="true" />

				<div class="cover-inner">
					<div class="cover-top">
						<img class="cover-title" src="/image/cover/YUNFEI%20HU..svg" alt="YUNFEI HU." />
						<div class="cover-info">
							<h2>ACADEMIC PORTFOLIO</h2>
							<p>Industrial Design · Master · 2024-2026</p>
							<p>Eindhoven University of Technology</p>
						</div>
					</div>

					<div class="cover-main">
						<nav class="cover-menu" aria-label="Sections">
							{coverMenu.map((item) => (
								<button class="cover-menu-row" type="button" data-cover-link data-nav-target={item.target}>
									<span>{item.label}</span>
									<span class="cover-menu-plus" aria-hidden="true">+</span>
								</button>
							))}
						</nav>

						<div class="cover-side">
							<img class="cover-photo" src="/image/cover/photo.svg" alt="Portrait of Yunfei Hu" />
							<img class="cover-rat" src="/image/cover/RatOnly.svg" alt="" aria-hidden="true" />
							<div class="cover-lang" data-cover-lang>
								<span>EN</span><span class="lang-divider">|</span>
								<button class="lang-cn" type="button" aria-label="中文版">中</button>
								<span class="lang-tip" role="status">敬请期待</span>
							</div>
							<div class="cover-socials" aria-label="Social links">
								<a href="https://dribbble.com/Blueowlfifi" target="_blank" rel="noreferrer" title="Dribbble">
									<svg viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="1.6"/><path d="M5 9.5c4.5 1.5 9.5 1 13-2M4.5 14c5-.5 10 1 13.5 5M9 3.5c3 3.5 5.5 9 6.5 16" stroke="currentColor" stroke-width="1.6"/></svg>
								</a>
								<a href="mailto:fiahu301@gmail.com" title="Email">
									<svg viewBox="0 0 24 24" fill="none"><rect x="3" y="5" width="18" height="14" rx="2" stroke="currentColor" stroke-width="1.6"/><path d="m4 7 8 6 8-6" stroke="currentColor" stroke-width="1.6"/></svg>
								</a>
								<a href="https://www.linkedin.com/in/yunfei-fia-hu-813b8b2b0/" target="_blank" rel="noreferrer" title="Linkedin">
									<svg viewBox="0 0 24 24" fill="none"><rect x="3" y="3" width="18" height="18" rx="2" stroke="currentColor" stroke-width="1.6"/><path d="M8 10.5V17M8 7.5v.5M12 17v-4a2.2 2.2 0 0 1 4.4 0V17" stroke="currentColor" stroke-width="1.6" stroke-linecap="round"/></svg>
								</a>
							</div>
						</div>
					</div>
				</div>
			</section>

			<div class="page-gap"><span>Identity</span></div>

			<!-- ====== Identity / Vision ====== -->
			{textPages.map((page, index) => (
				<>
					<section class="page page-content" data-page={page.id}>
						<div class="page-scroll" data-scroll>
							<div class="page-body text-body">
								<h2 class="page-heading">{page.heading}</h2>
								<div class="text-columns">
									{page.paragraphs.length > 0
										? page.paragraphs.map((paragraph) => <p>{paragraph}</p>)
										: <p class="placeholder-note">（内容待填写：public/content/{page.id}.txt）</p>}
								</div>
							</div>
						</div>
					</section>
					<div class="page-gap"><span>{index === 0 ? 'Vision' : 'Project / Main'}</span></div>
				</>
			))}

			<!-- ====== Project / Main ====== -->
			<section class="page page-content" data-page="project-main">
				<div class="page-scroll" data-scroll>
					<div class="page-body">
						<div class="project-grid">
							{main.map((project) => (
								<a class="project-card" href={`/project/${project.slug}/`}>
									<img src={project.coverImage} alt={`${project.shortTitle} cover`} loading="lazy" draggable="false" />
									<span class="project-card-bar">
										<span class="project-card-text">
											<strong>{project.displayTitle}</strong>
											<small>{formatProjectDate(project.info.time)}</small>
										</span>
										<span class="project-card-more">+ More</span>
									</span>
								</a>
							))}
							{main.length === 0 && <p class="placeholder-note">（暂无项目：请在 Info.txt 中加 Category: M11/M12/M21/FMP）</p>}
						</div>
					</div>
				</div>
			</section>

			<div class="page-gap"><span>Project / Course</span></div>

			<!-- ====== Project / Course ====== -->
			<section class="page page-content" data-page="project-course">
				<div class="page-scroll" data-scroll>
					<div class="page-body">
						<div class="project-grid">
							{course.map((project) => (
								<a class="project-card" href={`/project/${project.slug}/`}>
									<img src={project.coverImage} alt={`${project.shortTitle} cover`} loading="lazy" draggable="false" />
									<span class="project-card-bar">
										<span class="project-card-text">
											<strong>{project.shortTitle}</strong>
											<small>{formatProjectDate(project.info.time)}</small>
										</span>
										<span class="project-card-more">+ More</span>
									</span>
								</a>
							))}
						</div>
					</div>
				</div>
			</section>

			<div class="page-gap"><span>Project / Extracurricular</span></div>

			<!-- ====== Project / Extracurricular ====== -->
			<section class="page page-content" data-page="project-extracurricular">
				<div class="page-scroll" data-scroll>
					<div class="page-body">
						<div class="project-banner-list">
							{extracurricular.map((project) => (
								<a class="project-banner" href={`/project/${project.slug}/`}>
									<img src={project.coverImage} alt={`${project.shortTitle} cover`} loading="lazy" draggable="false" />
									<span class="project-card-bar">
										<span class="project-card-text">
											<strong>{project.shortTitle}</strong>
											<small>{formatProjectDate(project.info.time)}</small>
										</span>
										<span class="project-card-more">+ More</span>
									</span>
								</a>
							))}
							{extracurricular.length === 0 && <p class="placeholder-note">（暂无项目：请在 Info.txt 中加 Category: Extracurricular）</p>}
						</div>
					</div>
				</div>
			</section>

			<div class="page-gap"><span>Expertise Areas</span></div>

			<!-- ====== Expertise / Development ====== -->
			{tailTextPages.map((page, index) => (
				<>
					<section class="page page-content" data-page={page.id}>
						<div class="page-scroll" data-scroll>
							<div class="page-body text-body">
								<h2 class="page-heading">{page.heading}</h2>
								<div class="text-columns">
									{page.paragraphs.length > 0
										? page.paragraphs.map((paragraph) => <p>{paragraph}</p>)
										: <p class="placeholder-note">（内容待填写：public/content/{page.id === 'expertise' ? 'expertise-areas' : page.id}.txt）</p>}
								</div>
							</div>
						</div>
					</section>
					<div class="page-gap"><span>{index === 0 ? 'Development' : 'Contract'}</span></div>
				</>
			))}

			<!-- ====== Contract ====== -->
			<section class="page page-content" data-page="contract">
				<div class="page-scroll" data-scroll>
					<div class="page-body contract-body">
						<div class="contact-cards">
							{contactCards.map((card) => (
								<article class="contact-card">
									<h3>{card.title}</h3>
									<div class="scratch-card" data-scratch-card data-card-title={card.title}>
										<img class="scratch-qr" src={card.qr} alt={`${card.title} QR code`} draggable="false" />
										<canvas class="scratch-cover" data-scratch-cover></canvas>
									</div>
									{card.href
										? <a class="contact-detail" href={card.href} target="_blank" rel="noreferrer">{card.detail}</a>
										: <p class="contact-detail">{card.detail}</p>}
								</article>
							))}
						</div>
						<p class="copyright">© 2026 Yunfei Hu. All rights reserved.</p>
					</div>
				</div>
			</section>
		</div>

		<img class="rat-scrollbar" data-rat src="/image/ui/littleRat.svg" alt="" aria-hidden="true" hidden />
	</div>

	<script>
		// ======== 元素引用 ========
		const site = document.querySelector('[data-site]');
		const track = document.querySelector('[data-track]');
		const pages = Array.from(track.querySelectorAll('.page'));
		const ids = pages.map((page) => page.dataset.page);
		const rat = document.querySelector('[data-rat]');

		let active = 0;
		let flipping = false;
		let overscroll = 0;
		let overscrollTimer;
		const FLIP_MS = 720;

		const GAP = Number.parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--page-gap')) || 140;
		const THRESHOLD = GAP * 0.62; // 拖出超过留白的 62% 才翻页

		const pageTop = (index) => pages[index].offsetTop;

		function setTrack(y, animate) {
			track.style.transition = animate ? `transform ${FLIP_MS}ms cubic-bezier(0.22, 0.9, 0.3, 1)` : 'none';
			track.style.transform = `translateY(${-y}px)`;
		}

		function updateSidebarState() {
			const id = ids[active];
			const groupOf = {
				identity: 'piv', vision: 'piv',
				'project-main': 'project', 'project-course': 'project', 'project-extracurricular': 'project',
				expertise: 'expertise', development: 'development', contract: 'contract',
			};
			document.querySelectorAll('[data-nav-group]').forEach((group) => {
				group.classList.toggle('is-open', group.dataset.navGroup === groupOf[id]);
			});
			document.querySelectorAll('.nav-sub[data-nav-target]').forEach((sub) => {
				sub.classList.toggle('is-active', sub.dataset.navTarget === id);
			});
			document.querySelectorAll('[data-nav-subgroup]').forEach((subgroup) => {
				subgroup.classList.toggle('is-open', subgroup.dataset.navSubgroup === id);
			});
		}

		function goTo(index, animate = true) {
			const target = Math.max(0, Math.min(pages.length - 1, index));
			active = target;
			overscroll = 0;
			flipping = animate;
			setTrack(pageTop(active), animate);
			site.classList.toggle('is-cover', ids[active] === 'cover');
			history.replaceState(null, '', ids[active] === 'cover' ? window.location.pathname : `#${ids[active]}`);
			updateSidebarState();
			window.setTimeout(() => {
				flipping = false;
				updateRat();
			}, animate ? FLIP_MS : 0);
		}

		// ======== 阻尼翻页 ========
		function springBack() {
			overscroll = 0;
			setTrack(pageTop(active), true);
		}

		function handleDelta(deltaY, settleImmediately = false) {
			if (flipping) return;
			const scroller = pages[active].querySelector('[data-scroll]');

			if (scroller) {
				const atTop = scroller.scrollTop <= 0;
				const atBottom = scroller.scrollTop + scroller.clientHeight >= scroller.scrollHeight - 1;
				if ((deltaY > 0 && !atBottom) || (deltaY < 0 && !atTop)) {
					scroller.scrollTop += deltaY;
					if (overscroll !== 0) springBack();
					return;
				}
			}

			const direction = deltaY > 0 ? 1 : -1;
			const isEdge = (direction > 0 && active === pages.length - 1) || (direction < 0 && active === 0);
			if (isEdge) return;

			if (Math.sign(overscroll) !== direction && overscroll !== 0) overscroll = 0;
			overscroll += deltaY;

			// 橡皮筋：拖得越多阻力越大，最多拖出一个留白的距离
			const eased = GAP * (1 - 1 / (1 + Math.abs(overscroll) / 380));
			setTrack(pageTop(active) + direction * eased, false);

			if (eased > THRESHOLD) {
				goTo(active + direction);
				return;
			}

			window.clearTimeout(overscrollTimer);
			if (settleImmediately) return; // 触摸场景由 touchend 决定回弹
			overscrollTimer = window.setTimeout(springBack, 240);
		}

		window.addEventListener('wheel', (event) => {
			event.preventDefault();
			handleDelta(event.deltaY);
		}, { passive: false });

		let touchLastY = null;
		window.addEventListener('touchstart', (event) => {
			touchLastY = event.touches[0].clientY;
		}, { passive: true });
		window.addEventListener('touchmove', (event) => {
			if (touchLastY === null) return;
			event.preventDefault();
			const y = event.touches[0].clientY;
			handleDelta(touchLastY - y, true);
			touchLastY = y;
		}, { passive: false });
		window.addEventListener('touchend', () => {
			touchLastY = null;
			if (overscroll !== 0 && !flipping) springBack();
		});

		window.addEventListener('keydown', (event) => {
			if (event.key === 'PageDown') { event.preventDefault(); goTo(active + 1); }
			if (event.key === 'PageUp') { event.preventDefault(); goTo(active - 1); }
		});

		// ======== 导航 ========
		document.querySelectorAll('[data-cover-link], [data-nav-link]').forEach((link) => {
			link.addEventListener('click', (event) => {
				const target = link.dataset.navTarget;
				if (!target) return;
				event.preventDefault();
				goTo(ids.indexOf(target));
			});
		});

		// 初始定位（支持 /#project-main 之类的深链）
		const initial = ids.indexOf(decodeURIComponent(window.location.hash.replace(/^#/, '')));
		if (initial > 0) goTo(initial, false); else updateSidebarState();

		window.addEventListener('hashchange', () => {
			const target = ids.indexOf(decodeURIComponent(window.location.hash.replace(/^#/, '')));
			if (target >= 0 && target !== active) goTo(target);
		});

		window.addEventListener('resize', () => setTrack(pageTop(active), false));

		// ======== 中文版提示 ========
		document.querySelectorAll('.lang-cn').forEach((button) => {
			button.addEventListener('click', () => {
				const wrap = button.closest('[data-lang-switch], [data-cover-lang]');
				wrap?.classList.add('is-tip-visible');
				window.setTimeout(() => wrap?.classList.remove('is-tip-visible'), 1600);
			});
		});

		// ======== 小老鼠滚动条 ========
		// littleRat.svg 为横向素材（默认头朝右）：
		// 下滑 rotate(90deg) 头朝下；上滑 rotate(-90deg) 头朝上；停靠左侧时水平。
		let ratParkedLeft = false;

		function updateRat(direction) {
			const scroller = pages[active].querySelector('[data-scroll]');
			if (ids[active] === 'cover' || !scroller) { rat.hidden = true; return; }
			rat.hidden = false;

			const scrollable = scroller.scrollHeight - scroller.clientHeight;
			const ratio = scrollable > 0 ? scroller.scrollTop / scrollable : 0;
			const travel = window.innerHeight * 0.7;
			const top = window.innerHeight * 0.14 + ratio * travel;
			rat.style.top = `${scrollable > 0 ? top : window.innerHeight * 0.12}px`;

			if (direction === 1) rat.dataset.heading = 'down';
			else if (direction === -1) rat.dataset.heading = 'up';
			else if (!rat.dataset.heading) rat.dataset.heading = 'down';
		}

		pages.forEach((page) => {
			const scroller = page.querySelector('[data-scroll]');
			if (!scroller) return;
			let lastTop = 0;
			scroller.addEventListener('scroll', () => {
				const direction = scroller.scrollTop > lastTop ? 1 : -1;
				lastTop = scroller.scrollTop;
				if (!ratParkedLeft) updateRat(direction);
			}, { passive: true });
		});

		rat.addEventListener('click', () => {
			ratParkedLeft = !ratParkedLeft;
			rat.classList.toggle('is-parked', ratParkedLeft);
			if (!ratParkedLeft) updateRat();
		});

		updateRat();

		// ======== Contract 刮刮卡 ========
		document.querySelectorAll('[data-scratch-card]').forEach((card) => {
			const canvas = card.querySelector('[data-scratch-cover]');
			if (!(canvas instanceof HTMLCanvasElement)) return;
			const context = canvas.getContext('2d', { willReadFrequently: true });
			if (!context) return;
			const title = card.dataset.cardTitle ?? '';
			let isScratching = false;
			let resetTimer;

			function drawCover() {
				const size = Math.max(1, Math.round(card.clientWidth));
				const pixelRatio = window.devicePixelRatio || 1;
				canvas.width = Math.round(size * pixelRatio);
				canvas.height = Math.round(size * pixelRatio);
				canvas.style.width = `${size}px`;
				canvas.style.height = `${size}px`;
				context.setTransform(pixelRatio, 0, 0, pixelRatio, 0, 0);
				context.globalCompositeOperation = 'source-over';
				context.fillStyle = '#2c2c2c';
				context.fillRect(0, 0, size, size);
				context.fillStyle = '#ffe897';
				context.font = `${Math.round(size * 0.13)}px Helvetica, sans-serif`;
				context.textAlign = 'center';
				context.textBaseline = 'middle';
				context.fillText(title, size / 2, size / 2);
				card.classList.remove('is-cleared');
			}

			function scratchAt(event) {
				const rect = canvas.getBoundingClientRect();
				const x = event.clientX - rect.left;
				const y = event.clientY - rect.top;
				context.globalCompositeOperation = 'destination-out';
				context.beginPath();
				context.arc(x, y, 26, 0, Math.PI * 2);
				context.fill();
			}

			function clearedRatio() {
				const data = context.getImageData(0, 0, canvas.width, canvas.height).data;
				let transparent = 0;
				for (let i = 3; i < data.length; i += 4) if (data[i] < 16) transparent += 1;
				return transparent / (data.length / 4);
			}

			canvas.addEventListener('pointerdown', (event) => {
				event.preventDefault();
				isScratching = true;
				canvas.setPointerCapture(event.pointerId);
				scratchAt(event);
			});
			canvas.addEventListener('pointermove', (event) => { if (isScratching) scratchAt(event); });
			canvas.addEventListener('pointerup', (event) => {
				isScratching = false;
				if (canvas.hasPointerCapture(event.pointerId)) canvas.releasePointerCapture(event.pointerId);
				if (clearedRatio() >= 0.7) card.classList.add('is-cleared');
				window.clearTimeout(resetTimer);
				resetTimer = window.setTimeout(drawCover, 30000);
			});
			window.addEventListener('resize', drawCover);
			drawCover();
		});
	</script>
</Layout>

<style is:global>
	/* ======== 全局布局 ======== */
	.site { height: 100dvh; overflow: hidden; position: relative; }

	.track { will-change: transform; }

	.page {
		height: 100dvh;
		position: relative;
		background: var(--paper-texture) center top / cover no-repeat;
	}

	/* 内页比封面浅一档 */
	.page-content::before {
		content: '';
		position: absolute;
		inset: 0;
		background: rgba(255, 252, 235, 0.42);
		pointer-events: none;
	}

	.page-gap {
		height: var(--page-gap);
		display: flex;
		align-items: center;
		justify-content: center;
		background: var(--color-ink);
		color: var(--color-card-text);
		font-size: 14px;
		letter-spacing: 0.2em;
		text-transform: uppercase;
	}
	.page-gap span::after { content: ' \2193'; }

	/* ======== 顶栏 / 语言切换 ======== */
	.topbar {
		position: fixed;
		top: 0;
		right: 0;
		height: var(--topbar-h);
		display: flex;
		align-items: center;
		padding-right: calc(56 * var(--u));
		z-index: 40;
		transition: opacity 400ms ease;
	}
	.site.is-cover .topbar { opacity: 0; pointer-events: none; }

	.lang-switch, .cover-lang {
		position: relative;
		display: flex;
		align-items: center;
		gap: 8px;
		font-size: calc(16 * var(--u));
	}
	.lang-divider { opacity: 0.4; }
	.lang-cn {
		border: 0;
		background: none;
		font: inherit;
		color: var(--color-muted);
		cursor: pointer;
		padding: 0;
	}
	.lang-tip {
		position: absolute;
		top: 120%;
		right: 0;
		background: var(--color-ink);
		color: var(--color-card-text);
		font-size: 12px;
		padding: 4px 10px;
		border-radius: 4px;
		white-space: nowrap;
		opacity: 0;
		pointer-events: none;
		transition: opacity 200ms ease;
	}
	.is-tip-visible .lang-tip { opacity: 1; }

	/* ======== 侧边栏显隐（封面 ↔ 内页过渡） ======== */
	.sidebar {
		opacity: 1;
		transform: translateX(0);
		transition: opacity 500ms ease 180ms, transform 500ms ease 180ms;
	}
	.site.is-cover .sidebar {
		opacity: 0;
		transform: translateX(calc(-40 * var(--u)));
		pointer-events: none;
	}

	/* ======== 封面 ======== */
	.page-cover { overflow: hidden; }

	.cheese { position: absolute; pointer-events: none; }
	.cheese-lt { top: 0; left: 0; width: calc(200 * var(--u)); }
	.cheese-rt { top: 0; right: 0; width: calc(223 * var(--u)); }
	.cheese-lb { bottom: 0; left: 0; width: calc(157 * var(--u)); }
	.cheese-rb { bottom: 0; right: 0; width: calc(191 * var(--u)); }

	.cover-inner {
		position: relative;
		height: 100%;
		padding: calc(80 * var(--u)) calc(100 * var(--u));
		display: flex;
		flex-direction: column;
		gap: calc(40 * var(--u));
		transition: opacity 420ms ease, transform 420ms ease;
		transform-origin: top left;
	}
	/* 离开封面时整体向左收缩淡出 */
	.site:not(.is-cover) .cover-inner {
		opacity: 0;
		transform: translateX(calc(-60 * var(--u))) scale(0.94);
	}

	.cover-top {
		display: flex;
		justify-content: space-between;
		align-items: flex-start;
	}
	.cover-title { width: calc(559 * var(--u)); }
	.cover-info { text-align: left; width: calc(272 * var(--u)); }
	.cover-info h2 {
		margin: 0 0 calc(8 * var(--u));
		font-size: calc(20 * var(--u));
		letter-spacing: 0.08em;
	}
	.cover-info p {
		margin: 0;
		font-size: calc(11 * var(--u));
		color: var(--color-ink-soft);
	}

	.cover-main {
		flex: 1;
		display: flex;
		gap: calc(80 * var(--u));
		min-height: 0;
	}

	.cover-menu {
		flex: 1;
		display: flex;
		flex-direction: column;
		justify-content: space-between;
	}
	.cover-menu-row {
		display: flex;
		justify-content: space-between;
		align-items: center;
		flex: 1;
		border: 0;
		border-top: 1.5px solid var(--color-ink);
		background: none;
		font-family: var(--font-body);
		font-size: calc(32 * var(--u));
		color: var(--color-ink);
		cursor: pointer;
		text-align: left;
		padding: 0 calc(8 * var(--u));
		transition: background 180ms ease;
	}
	.cover-menu-row:last-child { border-bottom: 1.5px solid var(--color-ink); }
	.cover-menu-row:hover { background: rgba(255, 255, 255, 0.22); }
	.cover-menu-plus { font-size: calc(28 * var(--u)); font-weight: 300; }

	.cover-side {
		width: calc(320 * var(--u));
		position: relative;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: calc(20 * var(--u));
	}
	.cover-photo { width: calc(300 * var(--u)); }
	.cover-rat {
		position: absolute;
		right: calc(-60 * var(--u));
		bottom: calc(60 * var(--u));
		width: calc(152 * var(--u));
	}
	.cover-lang { justify-content: center; }
	.cover-socials {
		display: flex;
		gap: calc(20 * var(--u));
	}
	.cover-socials a { color: var(--color-ink); }
	.cover-socials svg { width: calc(24 * var(--u)); height: calc(24 * var(--u)); }

	/* ======== 内容页通用 ======== */
	.page-scroll {
		position: relative;
		height: 100%;
		overflow: hidden; /* 滚动由 JS 接管（scrollTop 仍可编程设置） */
	}

	.page-body {
		position: relative;
		min-height: 100%;
		padding:
			calc(96 * var(--u))
			calc(110 * var(--u))
			calc(80 * var(--u))
			calc(390 * var(--u));
	}

	.page-heading {
		margin: 0 0 calc(36 * var(--u));
		text-align: right;
		font-size: calc(40 * var(--u));
		font-weight: 700;
	}

	.text-columns p {
		margin: 0 0 calc(22 * var(--u));
		font-size: calc(19 * var(--u));
		line-height: 1.55;
		text-align: justify;
	}

	.placeholder-note { color: var(--color-muted); font-style: italic; }

	/* ======== 项目网格 ======== */
	.project-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: calc(36 * var(--u));
	}

	.project-card, .project-banner {
		display: block;
		text-decoration: none;
		background: var(--color-card-dark);
		overflow: hidden;
		border-radius: 4px;
	}
	.project-card img {
		display: block;
		width: 100%;
		aspect-ratio: 16 / 10;
		object-fit: cover;
	}
	.project-banner img {
		display: block;
		width: 100%;
		aspect-ratio: 32 / 11;
		object-fit: cover;
	}

	.project-card-bar {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: calc(12 * var(--u)) calc(16 * var(--u));
		color: var(--color-card-text);
	}
	.project-card-text { display: flex; flex-direction: column; gap: 2px; min-width: 0; }
	.project-card-text strong {
		font-size: calc(16 * var(--u));
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
	}
	.project-card-text small { font-size: calc(12 * var(--u)); opacity: 0.75; }
	.project-card-more {
		font-size: calc(13 * var(--u));
		border: 1px solid currentColor;
		border-radius: 999px;
		padding: 3px 10px;
		white-space: nowrap;
	}
	.project-card:hover .project-card-more,
	.project-banner:hover .project-card-more {
		background: var(--color-card-text);
		color: var(--color-card-dark);
	}

	.project-banner-list { display: flex; flex-direction: column; gap: calc(36 * var(--u)); }

	/* ======== Contract ======== */
	.contract-body {
		display: flex;
		flex-direction: column;
		justify-content: center;
		padding-right: calc(110 * var(--u));
	}

	.contact-cards {
		display: flex;
		justify-content: space-between;
		gap: calc(32 * var(--u));
	}
	.contact-card {
		display: flex;
		flex-direction: column;
		gap: calc(12 * var(--u));
		width: calc(180 * var(--u));
	}
	.contact-card h3 {
		margin: 0;
		font-size: calc(15 * var(--u));
		font-weight: 400;
	}
	.scratch-card {
		position: relative;
		width: 100%;
		aspect-ratio: 1;
		border-radius: 6px;
		overflow: hidden;
		background: #fff;
	}
	.scratch-qr { position: absolute; inset: 0; width: 100%; height: 100%; }
	.scratch-cover { position: absolute; inset: 0; touch-action: none; cursor: crosshair; }
	.scratch-card.is-cleared .scratch-cover { opacity: 0; transition: opacity 400ms ease; pointer-events: none; }
	.contact-detail {
		font-size: calc(12 * var(--u));
		color: var(--color-ink-soft);
		text-decoration: none;
		margin: 0;
	}
	a.contact-detail:hover { text-decoration: underline; }

	.copyright {
		margin-top: calc(70 * var(--u));
		text-align: center;
		font-size: calc(12 * var(--u));
		color: var(--color-muted);
	}

	/* ======== 小老鼠滚动条 ======== */
	.rat-scrollbar {
		position: fixed;
		right: calc(28 * var(--u));
		width: calc(82 * var(--u));
		z-index: 50;
		cursor: pointer;
		transition: top 160ms linear, right 900ms ease, left 900ms ease, transform 300ms ease;
	}
	.rat-scrollbar[data-heading='down'] { transform: rotate(90deg); }
	.rat-scrollbar[data-heading='up'] { transform: rotate(-90deg); }
	/* 停靠左侧：水平、头朝右 */
	.rat-scrollbar.is-parked {
		right: auto;
		left: calc(310 * var(--u));
		transform: rotate(0deg);
	}
</style>
